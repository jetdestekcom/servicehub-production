# JetDestek.com - Environment Variables
# Bu dosyayı .env.local olarak kopyalayın ve değerleri doldurun

# ===========================================
# ZORUNLU DEĞİŞKENLER
# ===========================================

# NextAuth Konfigürasyonu
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-super-secret-key-minimum-32-characters-long

# Veritabanı
DATABASE_URL="file:./dev.db"

# Uygulama URL'i
APP_URL=http://localhost:3000

# Güvenlik
JWT_SECRET=your-jwt-secret-key-minimum-32-characters-long
ENCRYPTION_KEY=your-encryption-key-minimum-32-characters-long

# ===========================================
# OPSIYONEL DEĞİŞKENLER
# ===========================================

# Google OAuth (Opsiyonel)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Stripe Ödeme Sistemi (Opsiyonel)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# AWS S3 Dosya Yükleme (Opsiyonel)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# Email Gönderimi (Opsiyonel)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@jetdestek.com

# SMS Gönderimi (Opsiyonel)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Google Maps (Opsiyonel)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Redis Cache (Opsiyonel)
REDIS_URL=redis://localhost:6379

# Monitoring (Opsiyonel)
SENTRY_DSN=your-sentry-dsn
LOG_LEVEL=info

# ===========================================
# GÜVENLİK NOTLARI
# ===========================================

# 1. Bu dosyayı asla Git'e commit etmeyin
# 2. Production'da güçlü, rastgele şifreler kullanın
# 3. NEXTAUTH_SECRET en az 32 karakter olmalı
# 4. JWT_SECRET ve ENCRYPTION_KEY farklı olmalı
# 5. Tüm API anahtarlarını güvenli tutun
# 6. Production'da HTTPS kullanın
# 7. Database URL'i production'da güvenli tutun

# ===========================================
# KURULUM TALİMATLARI
# ===========================================

# 1. Bu dosyayı .env.local olarak kopyalayın:
#    cp env.example .env.local

# 2. Zorunlu değişkenleri doldurun:
#    - NEXTAUTH_SECRET: Güçlü bir şifre oluşturun
#    - JWT_SECRET: Güçlü bir şifre oluşturun
#    - ENCRYPTION_KEY: Güçlü bir şifre oluşturun

# 3. Opsiyonel servisleri konfigüre edin:
#    - Google OAuth için Google Cloud Console'da proje oluşturun
#    - Stripe için Stripe Dashboard'dan anahtarları alın
#    - AWS S3 için AWS Console'dan bucket oluşturun
#    - Email için Gmail App Password oluşturun
#    - SMS için Twilio hesabı oluşturun
#    - Maps için Google Maps API anahtarı alın

# 4. Uygulamayı başlatın:
#    npm run dev

